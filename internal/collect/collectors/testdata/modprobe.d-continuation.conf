# Synthetic: a directive continues on the next line when the line ends in a
# backslash, and a "#" is a comment only at the START of a line.
install dccp \
	/bin/true
blacklist \
  hfsplus
install squashfs /bin/false # kmod keeps this inside the command
