# A directive continues on the next line when the line ends in a backslash,
# and a "#" starts a comment anywhere on a line.
install dccp \
	/bin/true
blacklist \
  hfsplus  # an Apple filesystem this host has no use for
# install cramfs /bin/false
