# synthetic /etc/pam.d/vsftpd whose list applies to one group only, so an
# account outside it is not checked against the file at all.
auth	required	pam_listfile.so item=user sense=deny file=/etc/ftpusers apply=@ftpgroup onerr=succeed
@include common-auth
