; Synthetic: an administrator's own file, with the two spellings sysctl.d(5)
; allows - the slash form of a key and the ignore-if-missing "-" prefix.
kernel/yama/ptrace_scope = 1
-kernel.unprivileged_bpf_disabled = 2

kernel.sysrq=0
   kernel.perf_event_paranoid   =   3
= nothing
no equals sign here
; the same key assigned twice: the last one in the file wins
kernel.perf_event_paranoid = 1
kernel.randomize_va_space = 2
