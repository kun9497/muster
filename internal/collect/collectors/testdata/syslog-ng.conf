# synthetic syslog-ng configuration; muster v1 does not parse this dialect.
@version: 3.35
source s_src { system(); internal(); };
destination d_syslog { file("/var/log/syslog"); };
log { source(s_src); destination(d_syslog); };
