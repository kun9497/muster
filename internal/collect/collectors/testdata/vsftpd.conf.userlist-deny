# synthetic vsftpd configuration whose user list is a DENY list, named
# explicitly.
anonymous_enable=NO
local_enable=YES
pam_service_name=vsftpd
userlist_enable=YES
userlist_file=/etc/vsftpd/user_list
