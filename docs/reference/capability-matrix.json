{
  "nonroot": {
    "denied": [
      "accounts.shadow_in_use",
      "cron.dirs",
      "cron.files",
      "files.etc_shadow.acl_entries",
      "files.etc_shadow.acl_present",
      "files.etc_sudoers.acl_present",
      "files.root_home.acl_present",
      "kernel.sysctl.bpf_jit_harden",
      "kernel.sysctl.protected_fifos",
      "kernel.sysctl.protected_hardlinks",
      "kernel.sysctl.protected_regular",
      "kernel.sysctl.protected_symlinks",
      "sudo.includedir",
      "sudo.secure_path",
      "walk.complete"
    ]
  },
  "no-systemd": {
    "unsupported": [
      "services.ssh.installed",
      "services.ntp.installed",
      "services.syslog.installed",
      "services.ftp.installed"
    ]
  },
  "container": {
    "unsupported": [
      "walk.suid_sgid",
      "walk.suid_sgid_unverified",
      "walk.world_writable",
      "walk.sticky_missing",
      "walk.unowned",
      "walk.hidden",
      "kernel.modules",
      "swap.encrypted"
    ]
  },
  "_notes": {
    "swap.encrypted": [
      "Unsupported in a container only because the host's swap is a FILE reached through an overlay root (the CI runner's /mnt/swapfile), whose source is not a block device the container's kernel lists (K-16); a host whose swap is a partition resolves it through sysfs and answers a value, in a container as anywhere else."
    ]
  }
}
