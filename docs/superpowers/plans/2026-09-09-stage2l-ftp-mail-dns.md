# Stage 2L — FTP, mail and DNS Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Enrol the twelve remaining daemon items of the KISA 2026 Unix guide — U-35, U-45, U-46, U-47, U-48, U-49, U-50, U-51, U-53, U-54, U-56, U-57 — through three small evidence collectors (`ftp`, `mail`, `dns`), three new rows of the services table and a `libwrap` presence fact, judging eight of them automatically and enrolling four as MANUAL with evidence. **Control set 52 → 64 (auto 55, partial 5, manual 4).**

**Architecture:** Three collectors that read configuration files only and never invoke a daemon binary (D14): `ftp` parses vsftpd's flat `key=value` file (plus a minimal proftpd/pure-ftpd model) into anonymous/TLS/user-list/banner leaves and the effective access-control files; `mail` reads postfix `main.cf` and sendmail's `O PrivacyOptions=` line into flat evidence leaves and one derived `expn_vrfy_restricted`; `dns` parses the `named.conf` include chain (the `options`, `zone` and `acl` statement kinds at top level, with `include` handled at any depth) into per-zone `transfer_restricted`/`update_restricted` records. Every construct outside the model counts in an `unmodelled` leaf and makes the judged leaves ABSENT → MANUAL (H-16: under-claim, never over-claim). Implementations are detected by configuration file AND binary (the 2I IR-7 lesson: a conffile left behind by a removed package must not drive a verdict). The four items whose criterion is a version/advisory match (U-45, U-49) or a semantics muster does not model (U-46, U-47) ship as `automation: manual` — the first manual controls in the set — gated by `applies_when` so a host without the daemon reads NOT_APPLICABLE.

**Tech Stack:** Go 1.25 (`internal/collect/collectors`, Linux build tag), YAML controls under `controls/service/`, the `fsAccess` test double, the lab host for the Linux-only suite.

**Spec:** `docs/superpowers/specs/2026-09-02-muster-design.md` (D09 params, D14 never invoke daemons, D24 no advisory matching, §5.6 embedded lists, §6.5 status order, §7.3 MANUAL below confidence). Scope brief: the session scratchpad `2l-scope-brief.md`. Boundary rulings from 2G (U-54/U-35 belong here) and 2H (the libwrap gate parked to U-56).

## Global Constraints

- **Branch base:** `stage2l-ftp-mail-dns` @ `f6ddbca` (post-2I main, PRE-2J). Plan 2J merges before this plan's last task; **Task 7 rebases onto the post-2J main** and only then reconciles the counts (52 → 64) and writes U-45/U-49, whose evidence gates cite 2J's `patch.*` keys (Ruling L-6). Registry entries go in ONE contiguous block at the END of `internal/facts/registry.yaml` (`LoadRegistry` rejects duplicate keys; 2J's block will sit above ours after the rebase). `since: 1`, `schema_version` stays 1, the facts golden diff is additions-only.
- **Ruling L-1 (scope):** twelve items, all `category: service`, ids `muster.service.{ftp_anonymous (U-35), mail_version (U-45), mail_user_execution (U-46), mail_relay (U-47), mail_expn_vrfy (U-48), dns_version (U-49), dns_zone_transfer (U-50), dns_dynamic_update (U-51), ftp_banner (U-53), ftp_unencrypted (U-54), ftpusers_permissions (U-56), ftpusers_root (U-57)}`. Automation: U-35/48/50/51/53/54/56/57 `auto`; U-45/46/47/49 `manual` (with `manual_reason`). KISA ids from `docs/reference/kisa/kisa_mapping.json`: U-35 ← 2021 U-20; U-45 ← U-30; U-46 ← U-32; U-47 ← U-31; U-48 ← U-70; U-49 ← U-33; U-50 ← U-34; U-51 new (`"2021": []`); U-53 new; U-54 ← U-61; U-56 ← U-63; U-57 ← U-64. Importance: U-35/45/46/47/49/50 상; U-48/51/54/57 중; U-53/56 하.
- **Ruling L-2 (no daemon invocation, no version facts):** none of the three collectors declares a command (`Needs: "none"`, `Declare.Commands` empty). No `vsftpd -v`, `named -v`, `postconf`. There is no `ftp.version`/`mail.version`/`dns.version` fact; version-to-advisory matching is never automated (D24) — U-45/U-49 are MANUAL, and the host's patch state (2J's `patch.metadata_age_s` / `patch.pending_security_count`) is in the snapshot for the operator; their gates never cite `patch.*` (Ruling L-6).
- **Ruling L-3 (implementation = conffile AND binary):** `ftp.implementation` is `vsftpd` iff `/etc/vsftpd.conf` or `/etc/vsftpd/vsftpd.conf` is readable AND `/usr/sbin/vsftpd` is present; `proftpd` iff `/etc/proftpd/proftpd.conf` or `/etc/proftpd.conf` AND `/usr/sbin/proftpd`; `pure-ftpd` iff `/usr/sbin/pure-ftpd` is present AND (`/etc/pure-ftpd/pure-ftpd.conf` is readable OR `Glob("/etc/pure-ftpd/conf/*")` is non-empty) — never a directory stat, which the declaration does not cover; else `none`. `mail.implementation`: `postfix` (`/etc/postfix/main.cf` + `/usr/sbin/postfix`), `sendmail` (`/etc/mail/sendmail.cf` + `/usr/sbin/sendmail.sendmail` or `/usr/sbin/sendmail-mta`), `exim` (`/etc/exim4/update-exim4.conf.conf` or `/etc/exim/exim.conf` + `/usr/sbin/exim4` or `/usr/sbin/exim`), else `none`. `dns.implementation`: `bind` (`/etc/bind/named.conf` or `/etc/named.conf` + `/usr/sbin/named`), `unbound` (`/etc/unbound/unbound.conf` + `/usr/sbin/unbound`), else `none`. A conffile without its binary → `none` with a reason naming the leftover file. The binary probes are `anyPresent` (logging.go) over the literal `Declare.Reads` entries — a symlink or a denied stat counts as present, which is what Debian's `/usr/sbin/sendmail-mta` symlink needs. (amended by L-20, L-23)
- **Ruling L-4 (H-16 under-claim):** each parser has an explicit model; a construct outside it increments `<collector>.unmodelled` and makes every JUDGED leaf of that collector ABSENT (`absent_means: manual`). vsftpd: every `/etc/vsftpd/*.conf` OTHER THAN `/etc/vsftpd/vsftpd.conf` → unmodelled (the glob matches the main file itself). proftpd: `<VirtualHost>`, `<Global>`, `<IfModule>` wrapping a modelled directive, `Include` outside the declaration → unmodelled. named.conf: `view`, an ACL name that is not defined in the parsed chain, a nested `{}` inside an address-match list, an `include` outside the declaration (at any depth) → unmodelled. Evidence leaves (`config_files`, raw parameter strings, `parse_complete`) stay `ok`. (amended by L-18, L-24)
- **Ruling L-5 (read guard):** every Stat/Glob/Read matches the collector's `Declare.Reads` (`path.Match`, one `*` per segment). A config-supplied path that is READ — vsftpd `userlist_file` and `banner_file`, PAM `pam_listfile … file=`, proftpd `Include`, named `include` — goes through the `declared()` probe (`sshd.go`): read when covered, otherwise RECORDED (path in the reason, `parse_complete false`/unmodelled) and never touched. A config-supplied path that is only NAMED — a named zone's `file`, vsftpd `chroot_list_file` (nothing judges chroot), `rsa_cert_file`, proftpd `TLSRSACertificateFile` — is recorded verbatim and never opened; a zone record's `file` field is evidence and never affects `parse_complete` or `unmodelled`. (rewritten by L-17)
- **Ruling L-6 (2J seam):** this plan does NOT quote or add `packages.installed`. U-45 and U-49 are written in Task 7, after the rebase, so that their descriptions and `manual_reason` can name 2J's `patch.*` leaves as the operator's evidence — but their `applies_when` gates cite `services.<x>.installed eq true` and `<x>.implementation present` ONLY: `present` does not gate on ok (I-21), and an `absent` `patch.metadata_age_s` on a no-cache host must never flip a MANUAL control to NOT_APPLICABLE. (Ruling L-11 explains why a manual control cannot list extra evidence facts in v1.)
- **Ruling L-7 (honest degradation, R220/H-17):** an ENOENT on any candidate is `absent`/`ok:false` as the model says; an EACCES on an existing file → `FromReadError` → `denied` (honest ERROR; vsftpd.conf is 0644 on both distros, `/etc/pam.d/vsftpd` 0644, `named.conf` 0640 root:named on RHEL → non-root `denied` — expected and honest); never `error` for an environment limitation; never `missing`. `collect-contract` (`--read-only --network none`, no daemons) must stay `run.complete == true`: with no conffiles every judged leaf is `absent` (or `ok:false` where the model says so) and every evidence leaf `ok`.
- **Ruling L-8 (services rows):** three rows appended to `var services` (services.go), registered as 5 keys each (`installed, active, unit_file_state, enabled, reachable`): `ftp` units `vsftpd.service`, `vsftpd.socket`, `vsftpd@.service`? — NO template units (probeUnits takes concrete names): `vsftpd.service`, `vsftpd.socket`, `proftpd.service`, `proftpd.socket`, `pure-ftpd.service`, `pure-ftpd-mysql.service`? — keep `pure-ftpd.service` only; `inetdNames: ftp`, `servers: vsftpd, proftpd, in.ftpd, pure-ftpd`, `ports: tcp 21`. `mail` units `postfix.service`, `postfix@-.service`, `sendmail.service`, `exim4.service`, `exim.service`; no inetd; `ports: tcp 25`. `dns` units `named.service`, `bind9.service`, `named-chroot.service`, `unbound.service`; no inetd; `ports: udp 53, tcp 53`. All `provesInstall: true` (genuinely new units, R63). Verified in the pre-flight: a row with `ports` gets `reachable` automatically, so all three rows are 5 keys each ⇒ 15 keys.
- **Ruling L-9 (libwrap):** `files.libwrap_present` (bool, collector `files`) = `collect.OK(anyPresent(a, libwrapPaths), src)` over `/usr/lib/x86_64-linux-gnu/libwrap.so.0`, `/usr/lib/aarch64-linux-gnu/libwrap.so.0`, `/usr/lib64/libwrap.so.0`, `/lib64/libwrap.so.0`, `/usr/lib/libwrap.so.0`; `anyPresent` counts a symlink or a denied stat as present, so `ok:false` only when every candidate is ENOENT. It is registered as evidence for the operator and for 2M's U-28 gate (the 2H S1 gap: RHEL 8+ ships no libwrap, so `tcp_wrappers=YES` and a deny-all `hosts.deny` restrict nothing there); no 2L control consumes it, and lint's unused-key note is informational. U-28 is NOT amended in this plan. (rewritten by L-19, L-20)
- **Ruling L-10 (mail scope):** no permission facts of the MTA binaries (`/usr/sbin/sendmail` is a symlink on postfix hosts and the no-follow read primitive refuses it — an `error` there would flip `run.complete`). U-46's evidence is the flat `authorized_submit_users` (postfix) / `privacy_options` (sendmail) leaves.
- **Ruling L-11 (MANUAL evidence in v1):** `internal/check/eval.go:85-92` returns at step 4 for `automation: manual` with `Reason = manual_reason` and whatever evidence `applies_when` accumulated (M10). `checks` are never evaluated for a manual control, and there is no `evidence:` field. Therefore a manual control's attached evidence is exactly its `applies_when` facts: use `[{ services.<x>.installed eq true }, { <x>.implementation present }]` (`implementation` is always `ok`, so the second clause cannot screen). The detailed leaves are in the snapshot for the operator. An `evidence:` list for manual controls is parked to 2M.
- **Ruling L-12 (references):** `references.kisa` only for all twelve. The four indexed RHEL 9 rules (`RHEL-09-215015` FTP package absent, `215020` sendmail absent, `215101` postfix present, `672050` bind crypto policy) judge package presence or crypto policy — opposite or unrelated polarity — and neither Ubuntu benchmark has an FTP/mail/DNS rule. No `make refindex`.
- **Ruling L-13 (fixtures and e2e, H-15):** every fixture address is RFC 5737 / `example.org`; `synthetic: true`; the eight auto controls are PASS in BOTH shared e2e fixtures; the four manual controls appear as the fixed string `"MANUAL"` in both e2e maps (the `muster.file.world_writable` precedent, `cmd/muster/e2e_test.go:200,264`) with their gate facts `ok:true` in both fixtures so the MANUAL branch is exercised; the waiver e2e literal (`Applied != 10 || Unknown != 1`) is untouched. A `manual` control is exempt from the pass-/fail- pair (`fixtures_test.go:107-113`) but ships `manual-*.json` and `na-*.json`; every fixture carries EVERY leaf the selected mechanism's `when`/`checks` and `applies_when` read, and every `na-*` fixture carries BOTH gate facts — a fixture that omits one is `ERROR missing_fact`, because every `applies_when` fact is screened before evaluation. Fixture zone names and hostnames are `example.org`/`example.net` ONLY — never a `.local`, `.lan`, `.corp`, `.internal`, `.intra` or `.priv` name (`view "internal"` is fine). (amended by L-28)
- **Ruling L-14 (grammar, verified in 2I):** `each` = `{fact, op: each, subject, where?, require}` with ONE `require` (two requirements = two `each` clauses); list params substitute QUOTED `"${name}"`; `mechanisms[]` has no `name` (use `#` comments); `applies_when op: present` holds for `ok:false` (gate with `eq true`); an `absent` fact read by the chosen mechanism screens the control via `absent_means` before clauses run; `mergeSoft`: `unsupported` beats `absent` (a MANUAL shape needs `absent`); `risk` ∈ none|restart_service|reboot_required|lockout_risk; `subject_kind` ∈ file|dir|user|group|unit|port|module|mount|key|facility|zone (list types only). "Mode ≤ NNN" is `op: in` over the subsets of NNN, whose values are decimal INTS and never octal strings: copy the shape of `controls/account/cron_permissions.yaml`, which already carries `params.allowed_modes: {type: list<int>, …}` with `require: {field: mode, op: in, expected: "${allowed_modes}"}`. (amended by L-15)
- **Pre-flight rulings L-15 … L-32 (folded 2026-09-09).** The pre-flight (`.superpowers/sdd/2026-09-09-stage2l-ftp-mail-dns/preflight-scan.md`) decoded all twelve controls and 70 fixtures through the real loader and engine; the rulings below fold its findings into the plan and amend L-1…L-14 where marked.
- **Ruling L-15 (B-1, U-56 mode list):** U-56's `params.allowed_modes` is `type: list<int>, default: [0, 32, 128, 160, 256, 288, 384, 416]` (the bit-subsets of 0640), with the `checks` block exactly as Task 5 now shows; `ftp.access_files[].mode` is the INT of the raw 0o7777 bits exactly as `writePermFacts` stores it (416 for 0640), and a record whose stat did not succeed carries `mode: -1, uid: -1, gid: -1`, never 0. The shape to copy is `controls/account/cron_permissions.yaml`; a `list<string>` param over an int field is `ERROR(internal_error)` on every fixture and lint does not catch it.
- **Ruling L-16 (B-2, unset `allow-transfer`):** when neither the zone nor `options` sets `allow-transfer`, the effective default comes from `/etc/os-release` (the dns collector declares `/etc/os-release` and `/usr/lib/os-release`): `ID=ubuntu` with VERSION_ID 22.04/24.04, `ID=debian` with VERSION_ID 12, and `ID` rhel/rocky/almalinux with VERSION_ID 9.* → `any` (BIND 9.16/9.18) → `transfer_restricted false` with a reason naming the default; any other distribution or version → `transfer_restricted` ABSENT → MANUAL, because BIND 9.20 turned outgoing transfers off by default. `allow-update` unset is `none` in every version → `update_restricted true`; U-50's description states both defaults and the 9.20 boundary, Task 4 gains the collector test `TestDnsUnsetTransferDefaultFollowsTheBuild` (Ubuntu 22.04 → false; an unknown ID → absent) and U-50 the fixture `manual-unset-unknown-build`.
- **Ruling L-17 (B-3, read vs named paths):** L-5 is rewritten — a config-supplied path that is READ goes through `declared()`, while a path that is only NAMED (a named zone's `file`, vsftpd `chroot_list_file` and `rsa_cert_file`, proftpd `TLSRSACertificateFile`) is recorded verbatim, never opened, and never affects `parse_complete` or `unmodelled`. Routing a zone's `file` through `declared()` would make `dns.zones` absent on every real BIND host, since zone files live outside any declaration.
- **Ruling L-18 (S-1, nested `include`):** `/etc/crypto-policies/back-ends/bind.config` joins the dns `Declare.Reads`, and `include "path";` is handled at ANY depth — declared → tokenised in place, its statements belonging to the enclosing block; undeclared → `unmodelled++` with the path in the reason. The "top-level statements only" wording is replaced: statement KINDS are still modelled at top level and `include` is the exception; Task 4 gains `TestDnsBindRhelChain` with the nested include (fragment `testdata/bind.config.rhel` holding `disable-algorithms`/`disable-ds-digests` only) and the RHEL top-level includes.
- **Ruling L-19 (S-2, the libwrap consumer):** U-56 stays the ftpusers-permission control (its lineage is 2021 U-63), and L-9 is rewritten: `files.libwrap_present` is registered as evidence for the operator and for 2M's U-28 gate, consumed by no 2L control, and lint's unused-key note is informational.
- **Ruling L-20 (S-3, presence probes):** `files.libwrap_present = collect.OK(anyPresent(a, libwrapPaths), src)` — `anyPresent` (logging.go) counts a symlink or a denied stat as present and yields `ok:false` only when every candidate is ENOENT; there is no denied branch, so "EACCES → denied" is dropped from `TestFilesLibwrapPresent`. L-3's binary probes are the same `anyPresent` over the literal `Declare.Reads` entries (the `/usr/sbin/sendmail-mta` symlink case).
- **Ruling L-21 (S-4, `pam_service_name`):** the PAM deny source is `/etc/pam.d/` + the `pam_service_name` value (default `vsftpd`), so declare `/etc/pam.d/vsftpd` AND `/etc/pam.d/ftp`. Any other name goes through `declared()` → recorded, and `root_denied` is absent unless `local_enable=NO` or an allow-list decides; Task 2 gains the testdata `vsftpd.conf.pam-ftp` and a case in `TestFtpUndeclaredPamListfileIsRecorded`.
- **Ruling L-22 (S-5, `stat_status`):** the `stat_status` vocabulary is 2I's five values `ok|absent|denied|error|undeclared` (`log_targets`), not 2E's three — `undeclared` is exactly the "recorded, never touched" case of L-5. The separate `exists` bool stays, because the `where` clause filters on it.
- **Ruling L-23 (S-6, pure-ftpd detection):** `pure-ftpd` iff `/usr/sbin/pure-ftpd` is present AND (`/etc/pure-ftpd/pure-ftpd.conf` is readable OR `Glob("/etc/pure-ftpd/conf/*")` is non-empty); there is no directory stat, since `/etc/pure-ftpd/conf/*` matches neither `/etc/pure-ftpd` nor `/etc/pure-ftpd/conf`. L-3 and Task 1 are corrected accordingly.
- **Ruling L-24 (S-7, extra vsftpd instances):** the extra-instance rule is "every `/etc/vsftpd/*.conf` OTHER THAN `/etc/vsftpd/vsftpd.conf` → unmodelled", because the glob matches the main file itself and every RHEL host would otherwise read MANUAL. L-4 and Task 1 are corrected accordingly.
- **Ruling L-25 (N-1, the Debian man page):** U-35's description and the comment of `TestFtpVsftpdAnonymousDefaultIsYes` state that Debian's man page says `anonymous_enable` "Default: NO" — a doc-only patch — while the compiled default is YES in both builds (`tunables.c`), so a reviewer holding the man page does not "fix" the model.
- **Ruling L-26 (N-2, sendmail `goaway`):** `mail.sendmail.privacy_options` keeps the literal `goaway` token AND appends its expansion, and only `novrfy` + `noexpn` are judged, since those two are present in every version; the registry description says the expansion list is version-dependent (8.14: authwarnings, noexpn, novrfy, noverb, needmailhelo, needexpnhelo, needvrfyhelo, nobodyreturn).
- **Ruling L-27 (N-3, key count):** the registry block is 50 keys — 15 services + 1 files + 17 ftp + 10 mail + 7 dns — not "≈47"; the File Structure line says 50.
- **Ruling L-28 (N-4, fixture names):** fixture zone names and hostnames are `example.org`/`example.net` ONLY — never `.local`, `.lan`, `.corp`, `.internal`, `.intra` or `.priv`, because gitleaks' internal-hostname rule scans every commit of the PR range; `view "internal"` (no dot) is fine. This is part of L-13 and of the Task 4/6 testdata notes.
- **Ruling L-29 (N-5, a green tree per task):** each control task keeps the whole tree green. Task 5 also bumps the two `47` literals in `cmd/muster/controls_test.go` to `52`, adds its five controls (PASS) to BOTH e2e maps with every leaf they read in BOTH shared fixtures, and regenerates `docs/reference/coverage.md`; Task 6 does the same for its five (52 → 57; four PASS plus `mail_user_execution` and `mail_relay` as the fixed `"MANUAL"` string with their gates `ok:true`); Task 7 then rebases (resolving the count literals, the e2e maps and coverage against 2J's 52, so ours becomes 62), adds U-45/U-49 (→ 64) and regenerates coverage once more.
- **Ruling L-30 (N-6, named evidence):** U-47's `manual_reason` and description name `services.mail.reachable` (a tcp/25 non-loopback listener) alongside `mail.postfix.inet_interfaces`, `mynetworks` and `smtpd_relay_restrictions`; U-35's description mentions that the `pure-ftpd-mysql`/`-ldap`/`-postgresql` variants are seen only through port 21.
- **Ruling L-31 (N-7, the test doubles):** the ftp, mail and dns doubles seed `stats` with `kind: "regular"` for every binary and `kind: "dir"` for `/run/systemd/system`, copying `loggingAccess` (logging_test.go:14–36), which derives the binary seeds from the conffile keys — I-20's "five maps" is only the minimum.
- **Ruling L-32 (N-8, the README count):** README line 137's "58 of 67 … the remaining 9 (mail, DNS and FTP …) listed as MANUAL" will be stale after 2L, since eight of those nine become auto; it is not touched here but logged as a parked follow-up for 2M (see Parked).
- **Ruling L-33 (gitleaks internal-hostname shape):** the real Debian fragment name `named.conf` + `.local` matches gitleaks' internal-hostname rule (`…\.(corp|internal|intra|lan|local|priv)`), and gitleaks scans every PR commit. The plan never spells that file name out; Task 4, which ships the Debian chain fixture that must contain the literal `include` line, adds an exact-literal allowlist entry for that one file name to the `internal-hostname-shape` rule in `.gitleaks.toml` (the 2H pattern: a literal, never a path allowlist) and verifies with `gitleaks git --no-banner` (8.24.3, via PowerShell) before committing; every other fixture name stays on example.org/example.net (L-28).
- **Determinism:** records sorted (access files by path; zones by name; config_files by path); no map iteration reaches a value; no `time.Now`. **Attribution:** no KISA/CIS text; descriptions in muster's own words. **Lab:** the LAB TOKEN is 2J's until 2J merges — 2L's Linux-only tests are cross-compiled (`GOOS=linux GOARCH=amd64 go vet ./... && go test -c ./internal/collect/collectors/ -o NUL`) until the token passes to 2L; CI runs them.
- **Parked to 2M:** README line 137's count sentence (L-32); an `evidence:` field for manual controls, so a MANUAL verdict can carry more than its `applies_when` facts (L-11); and a validation for a vacuously-selecting `each … where` clause, which today is a silent PASS when the `where` filters every element out.

## File Structure

- Create `internal/collect/collectors/ftp.go` — `ftp` collector: implementation, config chain, vsftpd flat parse, proftpd/pure-ftpd minimal models, access files, banner. `ftp_test.go` + `testdata/vsftpd.conf.*`, `proftpd.conf.*`, `pam.d.vsftpd.*`, `ftpusers.*`, `user_list.*`.
- Create `internal/collect/collectors/mail.go` — `mail` collector: implementation, `main.cf` flat parse (continuations), `sendmail.cf` `O PrivacyOptions`, `expn_vrfy_restricted`. `mail_test.go` + `testdata/main.cf.*`, `sendmail.cf.*`.
- Create `internal/collect/collectors/dns.go` — `dns` collector: implementation, named.conf tokenizer + block parser with includes at any depth and ACL resolution, zones, the os-release build default. `dns_test.go` + `testdata/named.conf.*`, `bind.config.rhel`, `os-release.*`.
- Modify `internal/collect/collectors/services.go` — three rows appended to `var services` (L-8).
- Modify `internal/collect/collectors/files.go` (the `files` collector) — `files.libwrap_present` (L-9), declared paths.
- Modify `internal/collect/collectors/register.go` — register `ftp`, `mail`, `dns`.
- Modify `internal/facts/registry.yaml` + `internal/facts/testdata/facts-schema.golden.json` — one contiguous EOF block of 50 keys (15 services + 1 files + 17 ftp + 10 mail + 7 dns; L-27).
- Create `controls/service/ftp_anonymous.yaml`, `ftp_banner.yaml`, `ftp_unencrypted.yaml`, `ftpusers_permissions.yaml`, `ftpusers_root.yaml`, `mail_expn_vrfy.yaml`, `mail_user_execution.yaml`, `mail_relay.yaml`, `dns_zone_transfer.yaml`, `dns_dynamic_update.yaml` (Tasks 5–6) and `mail_version.yaml`, `dns_version.yaml` (Task 7) + `controls/testdata/<id>/*.json`.
- Modify `cmd/muster/controls_test.go`, `cmd/muster/e2e_test.go`, `cmd/muster/testdata/full-{pass,fail}.json` and `docs/reference/coverage.md` (regenerated) in EVERY control task — Task 5 (47 → 52), Task 6 (52 → 57) and Task 7 (62 → 64 after the rebase) — so the tree is green at each commit (L-29).

## Interfaces (produced by this plan)

Registry keys (all `since: 1`; `sensitivity: public` unless noted; collector as named):

- `services.{ftp,mail,dns}.{installed,active,unit_file_state,enabled,reachable}` — 15 keys, collector `services`, the 2G shapes verbatim (`reachable` bool: the fixed port has a listener).
- `files.libwrap_present` bool — collector `files` (L-9).
- `ftp.implementation` string (`vsftpd|proftpd|pure-ftpd|none`, always ok); `ftp.config_files` list<string> (`subject_kind: file`; files actually read, sorted); `ftp.parse_complete` bool; `ftp.unmodelled` int; `ftp.local_enabled` bool (vsftpd `local_enable`, default NO; proftpd/pure-ftpd true); `ftp.anonymous_enabled` bool (vsftpd `anonymous_enable`, DEFAULT YES when unset — the compiled default; proftpd: an `<Anonymous` block exists; pure-ftpd: `NoAnonymous` is not `yes`); `ftp.tls_enforced` bool (vsftpd: `ssl_enable=YES` AND `force_local_logins_ssl`≠NO AND `force_local_data_ssl`≠NO (both default YES) AND (`anonymous_enable=NO` OR `force_anon_logins_ssl=YES` AND `force_anon_data_ssl=YES`); proftpd: `TLSEngine on` AND `TLSRequired on` at top level; pure-ftpd: `TLS` ≥ 2); `ftp.tcp_wrappers` bool (vsftpd `tcp_wrappers`, default NO; proftpd: absent; pure-ftpd: absent); `ftp.userlist_enable` bool (default NO), `ftp.userlist_deny` bool (default YES), `ftp.userlist_file` string (explicit value, else the distro default `/etc/vsftpd.user_list` (Debian build) / `/etc/vsftpd/user_list` (RHEL build) — whichever candidate exists; absent when neither); `ftp.access_files` list<record> `{path, role (pam_deny|userlist_deny|userlist_allow|ftpusers), exists, root_listed, mode, uid, gid, stat_status}` (`subject_kind: file`; sorted by path; `mode`, `uid` and `gid` are the INTS `writePermFacts` stores — `mode` is the raw 0o7777 bits, 416 for 0640 — and a record whose stat did not succeed carries `-1` in all three, never 0 (L-15); `stat_status` ∈ `ok|absent|denied|error|undeclared` (2I's `log_targets` vocabulary), with `exists` kept as the bool the `where` clause filters on (L-22); the PAM deny file(s) from `/etc/pam.d/<pam_service_name>` `pam_listfile.so … sense=deny file=X` (L-21), the vsftpd user list with its role from `userlist_deny`, proftpd's `/etc/ftpusers` when `UseFtpUsers` is not `off`); `ftp.access_file_present` bool (any record `exists`); `ftp.root_denied` bool (vsftpd: `local_enable=NO`, OR root listed in a `pam_deny`/`userlist_deny` file, OR `userlist_allow` exists and does NOT list root; proftpd: `RootLogin` is not `on` (default off) OR root listed in `/etc/ftpusers` with `UseFtpUsers` on; pure-ftpd: always true — `MinUID` ≥ 1 by design, evidence reason); `ftp.banner_source` string (`ftpd_banner|banner_file|serverident|default|none`); `ftp.banner_text` string (the configured text; the compiled default text is NOT invented — `default` source carries an empty text and the reason names the product); `ftp.banner_discloses_version` bool (vsftpd: `default` source → true; `ftpd_banner`/`banner_file` text matching `(?i)\b(vsftpd|proftpd|pure-?ftpd|wu-?ftpd)\b` → true, else false; proftpd: `ServerIdent off` → false, `ServerIdent on "text"` → the pattern, unset → true (the product's default ident names it); pure-ftpd → ABSENT "banner is not configurable in this version's model"). 17 keys; `sensitivity: internal` for `banner_text`.
- `mail.implementation` string (`postfix|sendmail|exim|none`); `mail.config_files` list<string>; `mail.postfix.inet_interfaces`, `mail.postfix.mynetworks`, `mail.postfix.smtpd_relay_restrictions`, `mail.postfix.smtpd_recipient_restrictions`, `mail.postfix.disable_vrfy_command`, `mail.postfix.authorized_submit_users` — string each, the raw `main.cf` value (`$name` references left unexpanded), ABSENT when the key is unset with the reason naming postfix's compiled default (`all`, computed, `permit_mynetworks, permit_sasl_authenticated, defer_unauth_destination`, empty, `no`, `static:anyone`); `mail.sendmail.privacy_options` list<string> (the `O PrivacyOptions=` list, sorted, keeping the literal `goaway` token AND appending its expansion; the registry description states that the expansion is version-dependent — 8.14 is `authwarnings, noexpn, novrfy, noverb, needmailhelo, needexpnhelo, needvrfyhelo, nobodyreturn` — and that only `novrfy` and `noexpn`, present in every version, are judged (L-26)); `mail.expn_vrfy_restricted` bool (postfix: `disable_vrfy_command` is `yes` — postfix implements no EXPN; sendmail: `novrfy` AND `noexpn` present after expansion; exim: ABSENT "exim ACLs are not modelled"). 10 keys.
- `dns.implementation` string (`bind|unbound|none`); `dns.config_files` list<string>; `dns.parse_complete` bool; `dns.unmodelled` int; `dns.options.allow_transfer` string (the rendered address-match list of `options { allow-transfer {…} }`; ABSENT when unset, with the reason naming the build default the collector read from `/etc/os-release` — `any` on a BIND 9.16/9.18 build, unknown on any other build (L-16)); `dns.options.allow_update` string (ABSENT "not set; the default is none in every version"); `dns.zones` list<record> `{name, type, file, allow_transfer, allow_update, transfer_restricted, update_restricted}` (`subject_kind: zone`; sorted by name; `type` ∈ primary|master|secondary|slave|forward|hint|stub|mirror|redirect; `file` is recorded verbatim and never opened (L-17); effective lists = zone-level else options-level; `transfer_restricted` = the effective list is set AND contains no `any` (an ACL name resolved through the chain; `key`/`localhost`/`localnets`/`none`/addresses restrict), and when NOTHING sets `allow-transfer` it follows the os-release build default — false on a 9.16/9.18 build, ABSENT → MANUAL on any other (L-16); `update_restricted` = `allow-update` unset/`none`/no `any`, or `update-policy` present; the whole list is ABSENT when `parse_complete` is false or `unmodelled` > 0). 7 keys.

Controls consume: U-35 `ftp.anonymous_enabled`; U-53 `ftp.banner_discloses_version`; U-54 `services.ftp.active` + `ftp.tls_enforced`; U-56 `ftp.access_file_present` + `each` over `ftp.access_files`; U-57 `ftp.root_denied`; U-48 `mail.expn_vrfy_restricted`; U-50/U-51 `each` over `dns.zones`; all gated by `services.<x>.installed eq true` (and `dns.implementation eq bind` for U-50/U-51 — unbound serves no zones).

---

### Task 1: services rows, `files.libwrap_present`, and the `ftp` collector core

**Files:** modify `services.go` (three rows), `files.go` (libwrap), `register.go`; create `ftp.go`, `ftp_test.go`, `testdata/vsftpd.conf.{debian,rhel,anon-default,tls,userlist-allow,extra-instance}`, `testdata/proftpd.conf.{stock,virtualhost,ident-off}`, `testdata/pure-ftpd.conf.stock`; registry block (15 services + 1 files + the `ftp.*` keys of this task: implementation, config_files, parse_complete, unmodelled, local_enabled, anonymous_enabled, tls_enforced, tcp_wrappers, userlist_enable, userlist_deny, userlist_file) + golden.

**Interfaces:** consumes `collect.Access`, `FromReadError`, `declared()`, `anyPresent`, `buildBegun`, the `fsAccess` double; produces the keys above.

- [ ] **Step 1: Failing tests** (`ftp_test.go`; `ftpAccess` seeds all five maps — the I-20 lesson — and seeds `stats` with `kind: "regular"` for every binary and `kind: "dir"` for `/run/systemd/system`, copying `loggingAccess` (logging_test.go:14–36), which derives the binary seeds from the conffile keys (L-31))
```go
// Debian stock vsftpd.conf: anonymous NO, local YES, ssl NO → tls_enforced false, tcp_wrappers absent-from-file → false.
func TestFtpVsftpdDebianStock(t *testing.T) { /* files: /etc/vsftpd.conf=vsftpd.conf.debian; stats: /usr/sbin/vsftpd kind regular */ }
// An empty vsftpd.conf leaves anonymous_enable at the compiled default YES (a real misconfiguration, never a guess).
// Debian's man page says "Default: NO" — a doc-only patch; tunables.c compiles YES in both builds (L-25).
func TestFtpVsftpdAnonymousDefaultIsYes(t *testing.T) {}
// ssl_enable=YES with the force_local_* defaults → tls_enforced true; force_local_data_ssl=NO → false.
func TestFtpVsftpdTLSEnforced(t *testing.T) {}
// /etc/vsftpd/vsftpd.conf plus /etc/vsftpd/extra.conf → unmodelled 1 (the main file is excluded from the glob, L-24) and the judged leaves absent.
func TestFtpVsftpdSecondInstanceIsUnmodelled(t *testing.T) {}
// Conffile without /usr/sbin/vsftpd → implementation none, reason names the leftover (L-3).
func TestFtpLeftoverConffileIsNotAnImplementation(t *testing.T) {}
// pure-ftpd: no directory stat — the binary plus a non-empty Glob("/etc/pure-ftpd/conf/*") is the implementation (L-23).
func TestFtpPureFtpdDetectedByGlob(t *testing.T) {}
// proftpd stock: anonymous false (no <Anonymous>), RootLogin unset; a <VirtualHost> block → unmodelled.
func TestFtpProftpdStockAndVirtualHost(t *testing.T) {}
// EACCES on an existing vsftpd.conf → every judged leaf denied with the path (C3), never a default.
func TestFtpUnreadableConfIsDenied(t *testing.T) {}
// No FTP at all → implementation none, judged leaves absent, config_files ok:[], Worst == ok.
func TestFtpNoDaemonIsAbsentNotMissing(t *testing.T) {}
// services rows: no-systemd → all fifteen new leaves unsupported (the 2G/2I table test extended).
func TestServicesFtpMailDnsRowsDegradeWithTheTable(t *testing.T) {}
// files.libwrap_present: any candidate present — a symlink counts (anyPresent) → true; every candidate ENOENT → ok:false. No denied branch (L-20).
func TestFilesLibwrapPresent(t *testing.T) {}
```
- [ ] **Step 2: Run red.** Cross-compile (`GOOS=linux GOARCH=amd64 go test -c ./internal/collect/collectors/ -o NUL`); the Linux run happens on the lab host once the LAB TOKEN is 2L's, else in CI.
- [ ] **Step 3: Implement `ftp.go`.** `Declare.Reads`: `/etc/vsftpd.conf`, `/etc/vsftpd/vsftpd.conf`, `/etc/vsftpd/*.conf`, `/etc/vsftpd/ftpusers`, `/etc/vsftpd/user_list`, `/etc/vsftpd.user_list`, `/etc/vsftpd.ftpusers`, `/etc/ftpusers`, `/etc/pam.d/vsftpd`, `/etc/pam.d/ftp`, `/etc/proftpd/proftpd.conf`, `/etc/proftpd.conf`, `/etc/proftpd/*.conf`, `/etc/proftpd/conf.d/*.conf`, `/etc/pure-ftpd/pure-ftpd.conf`, `/etc/pure-ftpd/conf/*`, `/usr/sbin/vsftpd`, `/usr/sbin/proftpd`, `/usr/sbin/pure-ftpd`. The implementation probes use `anyPresent` over the literal binary entries, and pure-ftpd is decided by the binary plus a readable `pure-ftpd.conf` or a non-empty `Glob("/etc/pure-ftpd/conf/*")` — no directory stat (L-20, L-23). vsftpd flat parse: `key=value` per line, `#` comments, keys case-insensitive, values `YES`/`NO` case-insensitive, LAST wins; the booleans above with their compiled defaults; every read file appended to `config_files`; every `/etc/vsftpd/*.conf` OTHER THAN `/etc/vsftpd/vsftpd.conf` is an extra instance → unmodelled (L-24). proftpd: line directives at top level (`ServerIdent`, `UseFtpUsers`, `RootLogin`, `TLSEngine`, `TLSRequired`, `<Anonymous` opener); `Include` → `declared()` → read or unmodelled; `<VirtualHost`/`<Global`/`<IfModule` → unmodelled (the directives inside are not attributed). pure-ftpd: `/etc/pure-ftpd/conf/<Name>` one-setting files (Debian) or `Name value` lines (RHEL). The judged leaves (`anonymous_enabled`, `tls_enforced`, `local_enabled`, `tcp_wrappers`, `userlist_*`) are ABSENT when `parse_complete` is false or `unmodelled` > 0, with the reason naming the construct.
- [ ] **Step 4: services rows + libwrap + registry + golden + gates.** `gofmt -l .`, `go vet ./...` (host + linux), `go test ./internal/facts`, `go run ./cmd/muster controls lint --references docs/reference` → "ok: 47 controls" (this base has 47), coverage -check.
- [ ] **Step 5: Commit.** "Add the ftp collector core, the ftp/mail/dns service rows and the libwrap presence fact".

### Task 2: FTP access files, root denial and banner

**Files:** modify `ftp.go`, `ftp_test.go`; testdata `pam.d.vsftpd.{debian,rhel,nolistfile}`, `pam.d.ftp.debian`, `ftpusers.{root,noroot}`, `user_list.{root,noroot}`, `vsftpd.conf.{banner,banner-file,userlist-deny,userlist-allow,local-disabled,pam-ftp}`; registry keys `ftp.access_files`, `ftp.access_file_present`, `ftp.root_denied`, `ftp.banner_source`, `ftp.banner_text`, `ftp.banner_discloses_version` + golden.

- [ ] **Step 1: Failing tests**
```go
// Debian: /etc/pam.d/vsftpd names /etc/ftpusers (sense=deny); root listed → root_denied true; access_files has the pam_deny record with mode/uid as ints.
func TestFtpAccessFilesDebianPamDeny(t *testing.T) {}
// RHEL: pam names /etc/vsftpd/ftpusers; userlist_enable=YES (deny) with /etc/vsftpd/user_list → two records.
func TestFtpAccessFilesRhelUserlistDeny(t *testing.T) {}
// userlist_deny=NO turns user_list into an allow-list: root NOT listed → root_denied true; root listed → false.
func TestFtpUserlistAllowSemantics(t *testing.T) {}
// No deny file lists root and no allow-list → root_denied ok:false (a definite finding, not absent).
func TestFtpRootNotDeniedIsFalse(t *testing.T) {}
// local_enable=NO → root_denied true with the reason "local logins disabled".
func TestFtpLocalDisabledDeniesRoot(t *testing.T) {}
// pam_service_name=ftp (vsftpd.conf.pam-ftp) → the declared /etc/pam.d/ftp is the deny source; any other name → declared() → recorded,
// root_denied absent (MANUAL); a pam_listfile file= outside the declaration → recorded, never read, stat_status undeclared (L-21, L-22).
func TestFtpUndeclaredPamListfileIsRecorded(t *testing.T) {}
// Banner: unset → source default, discloses true; ftpd_banner "Welcome" → false; "vsFTPd 3.0" → true; banner_file read via declared().
func TestFtpBannerDisclosure(t *testing.T) {}
// proftpd: ServerIdent off → false; unset → true; RootLogin on → root_denied false.
func TestFtpProftpdIdentAndRootLogin(t *testing.T) {}
```
- [ ] **Step 2: Run red** (cross-compile / lab when the token is ours).
- [ ] **Step 3: Implement.** The PAM deny source is `/etc/pam.d/` + the `pam_service_name` value (default `vsftpd` in both packaged builds; upstream is `ftp`): `/etc/pam.d/vsftpd` and `/etc/pam.d/ftp` are declared, and any other name goes through `declared()` → recorded, with `root_denied` absent unless `local_enable=NO` or an allow-list decides (L-21). Access records use the same stat primitive as `writePermFacts` for `mode/uid/gid`: `mode` is the INT of the raw 0o7777 bits, exactly `int(meta.Mode)` as `writePermFacts` stores it (416 for 0640), and a record whose stat did not succeed carries `mode: -1, uid: -1, gid: -1` (the home_dirs convention) — never 0, which reads as root-owned (L-15). `stat_status` is 2I's five-value vocabulary `ok|absent|denied|error|undeclared` (`log_targets`), and the separate `exists` bool stays for the `where` clause (L-22). `root_listed` = a non-comment, trimmed line equal to `root`. Banner: `ftpd_banner` wins over `banner_file` (vsftpd's precedence); `banner_file` is a config-supplied path (`declared()`; declare `/etc/issue.net` and `/etc/vsftpd/banner*` only — anything else is recorded). Disclosure regexp compiled once: `(?i)\b(vsftpd|proftpd|pure-?ftpd|wu-?ftpd)\b`.
- [ ] **Step 4: Registry + golden + gates.**
- [ ] **Step 5: Commit.** "Derive the FTP access-control files, root denial and banner disclosure".

### Task 3: the `mail` collector

**Files:** create `mail.go`, `mail_test.go`, `testdata/main.cf.{debian,relay-open,vrfy-disabled,continuation}`, `testdata/sendmail.cf.{goaway,novrfy-only,none}`; registry `mail.*` (10 keys) + golden; `register.go`.

- [ ] **Step 1: Failing tests** (the `mail` double seeds all five maps plus `stats` with `kind: "regular"` for every binary and `kind: "dir"` for `/run/systemd/system`, copying `loggingAccess` (L-31))
```go
// Debian stock main.cf: inet_interfaces "all", mynetworks "127.0.0.0/8 [::ffff:127.0.0.0]/104 [::1]/128", disable_vrfy_command absent (default no) → expn_vrfy_restricted false.
func TestMailPostfixDebianStock(t *testing.T) {}
// A continued value (line starting with whitespace) is joined; the last assignment wins.
func TestMailPostfixContinuationAndLastWins(t *testing.T) {}
// disable_vrfy_command = yes → expn_vrfy_restricted true.
func TestMailPostfixVrfyDisabled(t *testing.T) {}
// sendmail: O PrivacyOptions=goaway → privacy_options keeps the literal "goaway" AND appends the expansion; novrfy+noexpn → restricted true; novrfy alone → false (L-26).
func TestMailSendmailPrivacyOptions(t *testing.T) {}
// exim: implementation exim, expn_vrfy_restricted absent, config_files lists the files.
func TestMailEximIsDetectedNotParsed(t *testing.T) {}
// main.cf without /usr/sbin/postfix → none (L-3); EACCES → denied on every judged leaf; no MTA → absent, Worst ok.
func TestMailImplementationAndDegradation(t *testing.T) {}
```
- [ ] **Step 2: Run red.**
- [ ] **Step 3: Implement.** `Declare.Reads`: `/etc/postfix/main.cf`, `/etc/mail/sendmail.cf`, `/etc/exim4/update-exim4.conf.conf`, `/etc/exim/exim.conf`, `/usr/sbin/postfix`, `/usr/sbin/sendmail.sendmail`, `/usr/sbin/sendmail-mta`, `/usr/sbin/exim4`, `/usr/sbin/exim` (the binary probes are `anyPresent`, so Debian's `sendmail-mta` symlink counts — L-20). `main.cf` grammar: `name = value`, a line beginning with whitespace continues the previous value, `#` at column 0 is a comment, last wins; `$` references are NOT expanded (evidence). `sendmail.cf`: the first line matching `^O PrivacyOptions=` (also `^OpPrivacyOptions=`), comma-separated; the literal `goaway` token is KEPT in the list and its expansion appended (sorted), because membership is version-dependent — only `novrfy` and `noexpn`, present in every version, are judged (L-26); an unreadable `sendmail.cf` → denied leaves.
- [ ] **Step 4: Registry + golden + gates.**
- [ ] **Step 5: Commit.** "Add the mail collector: postfix and sendmail evidence with the EXPN/VRFY derivation".

### Task 4: the `dns` collector

**Files:** create `dns.go`, `dns_test.go`, `testdata/named.conf.{debian,rhel,open-transfer,acl,view,key,nested,include-undeclared}`, the include fragments (`bind.config.rhel` holding `disable-algorithms`/`disable-ds-digests` only, `named.conf.options`, the Debian local fragment, `named.rfc1912.zones`) and `testdata/os-release.{ubuntu2204,rhel9,unknown}`; registry `dns.*` (7 keys) + golden; `register.go`. Every fixture zone name and hostname is `example.org`/`example.net` (L-28).

- [ ] **Step 1: Failing tests** (the `dns` double seeds `stats` as L-31 describes)
```go
// Debian: named.conf includes named.conf.options (allow-transfer { none; }) and the `.local` fragment of Debian's named.conf chain (one primary zone) → zone transfer_restricted true (inherited), update_restricted true (unset = none).
func TestDnsBindDebianChain(t *testing.T) {}
// RHEL 9: /etc/named.conf with include "/etc/crypto-policies/back-ends/bind.config" INSIDE options {} (declared → tokenised in place)
// and the top-level named.rfc1912.zones / named.root.key includes → parse_complete true, unmodelled 0 (L-18).
func TestDnsBindRhelChain(t *testing.T) {}
// options allow-transfer unset, os-release ID=rhel VERSION_ID=9.4 → a primary zone reads transfer_restricted false (BIND 9.16 default any).
func TestDnsBindDefaultTransferIsOpen(t *testing.T) {}
// The unset default follows the build: os-release ubuntu 22.04 → transfer_restricted false; an unknown ID → transfer_restricted ABSENT → MANUAL (L-16).
func TestDnsUnsetTransferDefaultFollowsTheBuild(t *testing.T) {}
// zone-level allow-transfer { 192.0.2.0/24; } overrides an open options level; allow-update { any; } → update_restricted false; update-policy → true.
func TestDnsZoneLevelOverridesAndUpdatePolicy(t *testing.T) {}
// acl "xfer" { 198.51.100.0/24; }; allow-transfer { xfer; } → resolved, restricted true; an undefined name → unmodelled → zones absent.
func TestDnsAclResolution(t *testing.T) {}
// view "internal" { … } → unmodelled 1, zones absent, evidence leaves ok.
func TestDnsViewIsUnmodelled(t *testing.T) {}
// include "/etc/named/custom.conf" (declared) is read; include "/srv/x.conf" (undeclared, at any depth) → recorded, unmodelled++, parse_complete false.
func TestDnsIncludeGuard(t *testing.T) {}
// Comments in all three syntaxes, strings with braces inside, a key "tsig" reference → restricted true.
func TestDnsTokenizerEdges(t *testing.T) {}
// A zone's file path is recorded verbatim and never opened — parse_complete stays true and unmodelled 0 (L-17).
func TestDnsZoneFileIsRecordedNotRead(t *testing.T) {}
// unbound → implementation unbound, zones ok:[], options absent; named.conf without /usr/sbin/named → none; EACCES → denied.
func TestDnsImplementationAndDegradation(t *testing.T) {}
```
- [ ] **Step 2: Run red.**
- [ ] **Step 3: Implement.** `Declare.Reads`: `/etc/bind/named.conf`, `/etc/bind/named.conf.*`, `/etc/bind/*.conf`, `/etc/named.conf`, `/etc/named/*.conf`, `/etc/named.rfc1912.zones`, `/etc/named.root.key`, `/etc/named/*.zones`, `/etc/crypto-policies/back-ends/bind.config`, `/var/named/chroot/etc/named.conf`, `/etc/unbound/unbound.conf`, `/etc/os-release`, `/usr/lib/os-release`, `/usr/sbin/named`, `/usr/sbin/unbound`. Tokenizer: `{`, `}`, `;`, quoted strings, bare words; comments `//`, `#`, `/* */`. Parser: the modelled statement KINDS sit at top level — `options {…}`, `acl name {…}`, `zone "name" [class] {…}`, `view` → unmodelled; other top-level statements (`logging`, `key`, `controls`, `server`, `trust-anchors`, `managed-keys`, `statistics-channels`, `dnssec-policy`, `masters`/`primaries`) skipped as neutral with their block consumed by brace depth. `include "path";` is the EXCEPTION and is handled at ANY depth: declared → tokenised in place, its statements belonging to the enclosing block (stock RHEL 9 includes the crypto policy inside `options {}`); undeclared → `unmodelled++` with the path in the reason (L-18). Address-match list: elements `any`, `none`, `localhost`, `localnets`, an address/CIDR, `!elem`, `key name`, an ACL name (resolved from the chain, in any order — two passes), a nested `{}` → unmodelled. A zone's `file` is recorded verbatim and never opened (L-17). Effective lists per zone as in Interfaces; when NEITHER the zone nor `options` sets `allow-transfer`, the effective default comes from `/etc/os-release` (falling back to `/usr/lib/os-release`): `ID=ubuntu` with VERSION_ID 22.04/24.04, `ID=debian` with VERSION_ID 12, `ID` rhel/rocky/almalinux with VERSION_ID 9.* → `any` (BIND 9.16/9.18) → `transfer_restricted false` with a reason naming the default; any other distribution or version → `transfer_restricted` ABSENT with the reason "allow-transfer is not set and the BIND default depends on the version (any before 9.20, none from 9.20); set it explicitly" → MANUAL (L-16). `allow-update` unset is `none` in every version → `update_restricted true`. `zones` ABSENT when `parse_complete` false or `unmodelled` > 0.
- [ ] **Step 4: Registry + golden + gates.**
- [ ] **Step 5: Commit.** "Add the dns collector: the named.conf chain, ACLs and per-zone transfer and update restrictions".

### Task 5: the FTP controls U-35, U-53, U-54, U-56, U-57 + fixtures

**Files:** create `controls/service/ftp_anonymous.yaml`, `ftp_banner.yaml`, `ftp_unencrypted.yaml`, `ftpusers_permissions.yaml`, `ftpusers_root.yaml` and `controls/testdata/muster.service.<id>/*.json`; modify `cmd/muster/controls_test.go` (both `47` literals → `52`), `cmd/muster/e2e_test.go` (the count words and BOTH maps: these five PASS), `cmd/muster/testdata/full-{pass,fail}.json` (every leaf these five read, PASS-shaped, in BOTH shared fixtures) and `docs/reference/coverage.md` (regenerated) — the tree stays green at this commit (L-29).

All five: `category: service`, `automation: auto`, `requires_facts: ">=1"`, `absent_means: manual`, `applies_when: [{ fact: services.ftp.installed, op: eq, expected: true }]`, `references.kisa` only, `decision: D09`, `risk: restart_service` (U-56/U-57: `none` — file edits), EN+KO descriptions in muster's words that state the boundary (pure-ftpd/proftpd MANUAL shapes, the vsftpd compiled defaults).

- **U-35 `muster.service.ftp_anonymous`** (importance 상, kisa `{ "2026": ["U-35"], "2021": ["U-20"] }`): `checks: [{ fact: ftp.anonymous_enabled, op: eq, expected: false }]`. The description states that Debian's man page says "Default: NO" while the compiled default is YES in both builds, so an unset `anonymous_enable` is a real finding (L-25), and that the `pure-ftpd-mysql`/`-ldap`/`-postgresql` variants are seen only through port 21 (L-30). Fixtures: `pass-vsftpd-debian`, `pass-proftpd`, `pass-pure-ftpd`, `fail-vsftpd-default` (anonymous_enabled ok:true — the empty-config default), `fail-proftpd-anonymous-block`, `manual-unmodelled` (absent), `na-not-installed` (services.ftp.installed ok:false).
- **U-53 `muster.service.ftp_banner`** (하, `{"2026":["U-53"],"2021":[]}`): `checks: [{ fact: ftp.banner_discloses_version, op: eq, expected: false }]`. Fixtures: `pass-vsftpd-custom`, `pass-proftpd-ident-off`, `fail-vsftpd-default`, `fail-proftpd-default`, `manual-pure-ftpd` (absent), `na-not-installed`.
- **U-54 `muster.service.ftp_unencrypted`** (중, `{"2026":["U-54"],"2021":["U-61"]}`): two `mechanisms` — (1) `when: [{ services.ftp.active eq false }]` → `checks: [{ services.ftp.active eq false }]` (an installed but stopped FTP is not an unencrypted service); (2) `when: [{ services.ftp.active eq true }]` → `checks: [{ ftp.tls_enforced eq true }]`. When NO mechanism applies — `services.ftp.active` unsupported while `services.ftp.installed` is ok:true — the verdict is MANUAL "no mechanism applies to this host", not NOT_APPLICABLE (verified in the pre-flight; on a real no-systemd host `installed` is unsupported too, so `applies_when` screens to NA first). Fixtures: `pass-inactive`, `pass-vsftpd-tls`, `fail-vsftpd-plain`, `fail-vsftpd-anon-plain` (ssl on, anonymous on without force_anon_*), `manual-proftpd-unmodelled` (tls_enforced absent), `manual-active-unsupported` (installed ok:true, active unsupported), `na-not-installed`.
- **U-56 `muster.service.ftpusers_permissions`** (하, `{"2026":["U-56"],"2021":["U-63"]}`): the param is a list of INTS over the int mode the collector stores (L-15) — copy `controls/account/cron_permissions.yaml`, not `passwd_permissions.yaml`:
```yaml
params:
  allowed_modes: { type: list<int>, default: [0, 32, 128, 160, 256, 288, 384, 416], description: access-file modes at 0640 or stricter (bit-subsets of 0640) }
checks:
  - { fact: ftp.access_file_present, op: eq, expected: true }
  - { fact: ftp.access_files, op: each, subject: path, where: { field: exists, op: eq, expected: true }, require: { field: uid, op: eq, expected: 0 } }
  - { fact: ftp.access_files, op: each, subject: path, where: { field: exists, op: eq, expected: true }, require: { field: mode, op: in, expected: "${allowed_modes}" } }
```
  Fixtures: `pass-debian`, `pass-rhel-userlist`, `fail-world-readable` (mode 420 = 0644 → FAIL), `fail-not-root-owned`, `fail-no-access-file` (access_file_present ok:false), `manual-unmodelled`, `na-not-installed`.
- **U-57 `muster.service.ftpusers_root`** (중, `{"2026":["U-57"],"2021":["U-64"]}`): `checks: [{ fact: ftp.root_denied, op: eq, expected: true }]`. Fixtures: `pass-pam-deny`, `pass-userlist-allow`, `pass-local-disabled`, `pass-proftpd-rootlogin-off`, `fail-root-not-listed`, `fail-proftpd-rootlogin-on`, `manual-undeclared-listfile` (absent), `na-not-installed`.

- [ ] **Step 1:** write the five YAMLs; `go run ./cmd/muster controls lint --references docs/reference` → "ok: 52 controls" (47 + 5). If a YAML fails to decode, STOP and report — never drop a clause.
- [ ] **Step 2:** write the fixtures with every leaf the selected mechanism reads, and both gate facts in every `na-*` (L-13); `go test ./internal/controls/...` → every fixture yields its prefix status; run the real binary on the `manual-*`/`na-*`/`fail-*` ones and confirm the reason names the cause.
- [ ] **Step 3: Count seams (L-29).** Bump both `47` literals in `cmd/muster/controls_test.go` to `52`; add the five controls as PASS to BOTH e2e maps in `cmd/muster/e2e_test.go` (the count words too) and every leaf they read to BOTH `cmd/muster/testdata/full-{pass,fail}.json`; `go run ./tools/coverage`; `go test ./...` green.
- [ ] **Step 4: Commit.** "Add the FTP controls: anonymous access, banner disclosure, unencrypted service, access-file permissions and root denial".

### Task 6: the mail and DNS controls U-46, U-47, U-48, U-50, U-51 + fixtures

**Files:** create `controls/service/mail_expn_vrfy.yaml`, `mail_user_execution.yaml`, `mail_relay.yaml`, `dns_zone_transfer.yaml`, `dns_dynamic_update.yaml` + fixtures (zone names `example.org`/`example.net` only, L-28); modify `cmd/muster/controls_test.go` (both literals `52` → `57`), `cmd/muster/e2e_test.go` (the count words and BOTH maps: U-48/U-50/U-51 PASS plus `mail_user_execution` and `mail_relay` as the fixed `"MANUAL"` string), `cmd/muster/testdata/full-{pass,fail}.json` (every leaf the three auto controls read, PASS-shaped, and the two manual gates `installed ok:true` + `implementation ok`, in BOTH shared fixtures) and `docs/reference/coverage.md` (regenerated) — the tree stays green at this commit (L-29).

- **U-48 `muster.service.mail_expn_vrfy`** (auto, 중, `{"2026":["U-48"],"2021":["U-70"]}`): `applies_when: [{ services.mail.installed eq true }]`; `checks: [{ mail.expn_vrfy_restricted eq true }]`; `absent_means: manual`. Fixtures: `pass-postfix`, `pass-sendmail-goaway`, `fail-postfix-default`, `fail-sendmail-novrfy-only`, `manual-exim` (absent), `na-not-installed`.
- **U-46 `muster.service.mail_user_execution`** (MANUAL, 상, `{"2026":["U-46"],"2021":["U-32"]}`) and **U-47 `muster.service.mail_relay`** (MANUAL, 상, `{"2026":["U-47"],"2021":["U-31"]}`): `automation: manual`, `manual_reason` (EN, one sentence naming what the operator checks — U-46: whether ordinary users can run the MTA's queue/submission commands, from `mail.postfix.authorized_submit_users` / `mail.sendmail.privacy_options` `restrictqrun`/`restrictmailq`; U-47: whether the MTA relays for off-host clients, from `services.mail.reachable` (a tcp/25 non-loopback listener) together with `mail.postfix.{inet_interfaces,mynetworks,smtpd_relay_restrictions}` / sendmail's access map — not modelled; L-30), `applies_when: [{ services.mail.installed eq true }, { mail.implementation, op: present }]` (L-11), NO `checks`, `requires_facts: ">=1"`, no `absent_means`. Fixtures: `manual-postfix` (installed ok:true, implementation ok), `na-not-installed` (both gate facts present, L-13). Lint requires `manual_reason`; the fixtures test exempts the pass/fail pair.
- **U-50 `muster.service.dns_zone_transfer`** (auto, 상, `{"2026":["U-50"],"2021":["U-34"]}`) and **U-51 `muster.service.dns_dynamic_update`** (auto, 중, `{"2026":["U-51"],"2021":[]}`): `applies_when: [{ services.dns.installed eq true }, { dns.implementation eq bind }]`; U-50 `checks: [{ fact: dns.zones, op: each, subject: name, where: { field: type, op: in, expected: [primary, master] }, require: { field: transfer_restricted, op: eq, expected: true } }]`; U-51 the same with `update_restricted`. `absent_means: manual`. Fixtures (each): `pass-restricted`, `pass-no-primary-zones` (only hint/secondary → vacuous), `fail-open` (a primary zone with an explicit `any`), `manual-view` (zones absent), `na-unbound` (implementation unbound), `na-not-installed`; U-50 also `manual-unset-unknown-build` (nothing sets `allow-transfer` on a build the collector does not know → `transfer_restricted` absent, L-16). U-50's description states BOTH BIND defaults (`allow-transfer` any before 9.20 and none from 9.20; `allow-update` none in every version), that the collector reads the build from `/etc/os-release` and reads MANUAL on any other build, and that views read MANUAL.

- [ ] **Step 1:** YAMLs; lint → "ok: 57 controls".
- [ ] **Step 2:** fixtures; `go test ./internal/controls/...`; the MANUAL invariant (reason + evidence) holds for U-46/U-47's `manual-*` fixtures through the real binary.
- [ ] **Step 3: Count seams (L-29).** Bump both literals in `cmd/muster/controls_test.go` to `57`; add the five controls to BOTH e2e maps (three PASS + the two `"MANUAL"` strings) with their leaves and gates in BOTH shared fixtures; `go run ./tools/coverage`; `go test ./...` green.
- [ ] **Step 4: Commit.** "Add the mail and DNS controls: EXPN/VRFY, user execution, relay, zone transfer and dynamic update".

### Task 7: rebase onto the post-2J main, U-45/U-49, and the reconciliation 62 → 64

**Files:** `git rebase main` (after 2J merges) — resolving the count literals, the e2e maps and `docs/reference/coverage.md` against 2J's 52, so ours becomes 62; create `controls/service/mail_version.yaml`, `dns_version.yaml` + fixtures; modify `cmd/muster/controls_test.go` (`62` → `64`), `cmd/muster/e2e_test.go` and `cmd/muster/testdata/full-{pass,fail}.json` (U-45/U-49 as the fixed `"MANUAL"` string with their gates `installed ok:true` + `implementation ok`), `docs/reference/coverage.md` (regenerated once more → line 4 `64 of 67 items enrolled (auto 55, partial 5, manual 4).`). The README pair is untouched: its 58-of-67 sentence is a parked follow-up for 2M (L-32).

- [ ] **Step 1: Rebase.** `git rebase main`; resolve `registry.yaml` by keeping 2J's block ABOVE ours (ours stays last), `go test ./internal/facts` at once (duplicate keys rejected), golden `-update`, `services.go` rows (2J touches no rows), `register.go` (append); resolve `cmd/muster/controls_test.go`, `cmd/muster/e2e_test.go`, the two shared fixtures and `coverage.md` against 2J's 52 — ours (57) plus 2J's five becomes **62**, with both plans' controls in both maps. Confirm `git log` shows 2J's commits below ours, `git diff main --stat` lists only 2L files, and `go test ./...` is green before anything new is written.
- [ ] **Step 2: U-45 `muster.service.mail_version`** (MANUAL, 상, `{"2026":["U-45"],"2021":["U-30"]}`) and **U-49 `muster.service.dns_version`** (MANUAL, 상, `{"2026":["U-49"],"2021":["U-33"]}`): `manual_reason` names the check (the MTA/BIND package is at the vendor's patched level; muster never matches versions to advisories — D24 — and the host's patch state is in `patch.pending_security_count` / `patch.metadata_age_s`), `applies_when: [{ services.<x>.installed eq true }, { <x>.implementation present }]`, fixtures `manual-installed` and `na-not-installed` (both gate facts in each). Bump the count literals 62 → 64; lint → "ok: 64 controls".
- [ ] **Step 3: Count seams and fixtures** as listed; add U-45/U-49 to both e2e maps as `"MANUAL"` with their gates in both shared fixtures; `go test ./...`; coverage regenerated and `-check`; `GOOS=linux GOARCH=amd64 go vet ./... && go build ./...`; the standing secret guard over the whole branch → 0 matches.
- [ ] **Step 4: Commit.** "Enrol the FTP, mail and DNS controls in the coverage and end-to-end snapshots".

## Self-Review

- **Spec coverage:** twelve items → twelve controls (Tasks 5, 6, 7); three collectors (Tasks 1–4); the 2G/2H boundary rulings honoured (U-54/U-35 here; the libwrap fact registered as operator evidence and as 2M's U-28 gate, L-9/L-19); D14 (no daemon invocation) and D24 (no advisory matching) honoured by L-2; §6.5 order (applies_when before manual) relied on per `eval.go:65-92`.
- **Placeholder scan:** none — every test has a name and a stated assertion; every key has a type and a stated default/absent reason; the YAML shapes reuse the verified 2I/2H grammar.
- **Type consistency:** `ftp.access_files` record fields are the same in Task 2 (producer) and Task 5 (`each` consumer: `exists` bool, `uid` and `mode` ints, subject `path`); `dns.zones` fields in Task 4 and Task 6 (`type`, `transfer_restricted`, `update_restricted`, subject `name`); `services.<x>.installed` from Task 1 gates every control; `mail.expn_vrfy_restricted` in Tasks 3 and 6.
- **Pre-flight:** the scan decoded all twelve controls and 70 fixtures through the real loader and engine; its findings are folded in as rulings L-15…L-32, and the earlier known-risk list (the mode representation, `each … where` over a filtered record, the vsftpd compiled defaults, sendmail `goaway`, the BIND transfer default and the fixtures test on a `manual` control) is resolved by them.
